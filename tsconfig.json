{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    "baseUrl": "./src",          // src 디렉토리를 기준으로
    "paths": {
      "@/*": ["*"]               // "@/foo" → "src/foo"
    },
    // ... 그 외 기존 설정
  }
}
